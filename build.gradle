group 'JSON-RPC-Java'
version '1.0-SNAPSHOT'

allprojects {
    apply plugin: 'java'
    sourceCompatibility = 1.8
    repositories {
        mavenCentral()
    }
}

dependencies {
    compile project(':jsonrpcsimple')

    testCompile group: 'junit', name: 'junit', version: '4.11'
}
